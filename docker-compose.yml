version: '3'
services:
  php:
    working_dir: /var/www/weather
    build:
      context: './docker/php/'
    volumes:
      - '.:/var/www/weather'
    env_file:
      - .env.dev
    ports:
      - "8000:8000"
    command: php -S 0.0.0.0:8000 -t public